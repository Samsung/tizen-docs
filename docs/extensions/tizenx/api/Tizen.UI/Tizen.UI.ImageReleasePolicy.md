### [Tizen.UI](Tizen.UI.md 'Tizen.UI')

## ImageReleasePolicy Enum

Enumeration for ImageReleasePolicy.

```csharp
public enum ImageReleasePolicy
```
### Fields

<a name='Tizen.UI.ImageReleasePolicy.Destroyed'></a>

`Destroyed` 1

The image is released when the view is destroyed.

<a name='Tizen.UI.ImageReleasePolicy.Detached'></a>

`Detached` 0

The image is released when the view is detached from the window.

<a name='Tizen.UI.ImageReleasePolicy.Never'></a>

`Never` 2

The image is never released.







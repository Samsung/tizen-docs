### [Tizen.UI](Tizen.UI.md 'Tizen.UI')

## FittingMode Enum

Enumeration for FittingMode.

```csharp
public enum FittingMode
```
### Fields

<a name='Tizen.UI.FittingMode.Center'></a>

`Center` 3

Center the image in the view without scaling.

<a name='Tizen.UI.FittingMode.Fill'></a>

`Fill` 1

Fill the entire available space by stretching the image regardless of the aspect ratio.

<a name='Tizen.UI.FittingMode.FitHeight'></a>

`FitHeight` 4

Scale the image to fit the height of the available space while maintaining its aspect ratio.

<a name='Tizen.UI.FittingMode.FitWidth'></a>

`FitWidth` 5

Scale the image to fit the width of the available space while maintaining its aspect ratio.

<a name='Tizen.UI.FittingMode.ScaleToFill'></a>

`ScaleToFill` 2

Scale the image to fill the view. Some parts may be clipped in order to fill the view.

<a name='Tizen.UI.FittingMode.ScaleToFit'></a>

`ScaleToFit` 0

Scale the image to fit within the available space without changing its aspect ratio.







# Page

A site-root route with no file here is legitimate — the page behind it is
published by another pipeline.

[The .NET API reference](/application/dotnet/api/Tizen.Applications)

Media has no other pipeline, so these two are simply broken.

<img src="/docs/guides/media/gone.png" alt="A redundant docs/ prefix, and the file is gone">

<img src="/guides/media/absent.png" alt="A site-root reference to an image that is not here">
